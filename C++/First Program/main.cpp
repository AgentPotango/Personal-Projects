#include <iostream> /* including the input output stream */

using namespace std; /* Using the standard library */

int main() /* Main Method needed by all C++ programs */
{
    /* cout (Output Stream Object) << means output the following.
       << endl ends the line
       << & >> are stream insertion operators  */
    cout << "Hello world!" << endl;

    /*returns an interger due to the main being of type integer */
    return 0;
}
