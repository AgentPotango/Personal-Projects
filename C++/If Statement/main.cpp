#include <iostream>

using namespace std;

int main()
{
    /* Relational operatoes: > (greater than)
                             < (less than)
                             == (identical to)
                             != (not equal to)
    */

    if(5 % 4 != 1)
    {
        cout << "I am Awesome";
    }
    else
    {
        cout << "I am not so Awesome...";
    }
    return 0;
}
